# Youtube 项目更新文档

更新时间：2026-06-01

这个文档用于长期记录本程序的更新方向、已经完成的功能、待做功能、项目文件结构和每次处理视频时需要保留的资料。以后程序继续更新时，优先把变化追加到这里。

## 2026-06-01 在线识别整理

- 完全移除程序里的本地 Faster-Whisper 识别入口和后端调用逻辑。
- 字幕工作台的音视频识别只保留在线 API：火山极速版和火山标准版 URL。
- 新建项目目录改为中文命名：`00_源文件`、`01_视频`、`02_音频`、`03_字幕`、`04_简介`、`10_翻译`、`30_烧录`、`40_切片`；旧英文目录保留兼容，不自动删除。
- 字幕烧录改为后处理按钮：自动使用当前下载视频和当前翻译字幕，输出翻译视频到 `30_烧录`，不覆盖源视频。
- 字幕位置设置新增可输入参数：位置参数 `1-9` 和垂直边距像素值，可配合预览拖拽和九宫格按钮使用。
- 封面下载统一转换为 `.jpeg`；下载流程里的封面和单独“封面”按钮都使用 JPEG 输出。
- 视频切片路径入口支持拖拽本地视频文件或目录；拖入目录时自动选择目录里最近的视频文件，不复制源文件。
- 视频切片路径入口增加“粘贴路径”“选择文件”“选择目录”按钮，复制地址后可直接导入路径。
- 当浏览器拖拽不给完整路径时，拖拽单个文件会自动走上传读取/在线识别流程；目录仍通过“选择目录”保留原路径。
- 本地音频识别项目标题会自动去掉下载/抽取生成的 `.audio` 后缀，避免字幕生成到单独的 `xxx.audio` 项目文件夹。
- 页面默认使用火山极速版；旧浏览器缓存里的 `local` 识别方式会自动切回火山极速版。
- “字幕对比翻译”移动到在线字幕工作台内，只作用于字幕翻译，不再放在简介设置区域里。
- 视频切片增加“导入分段”按钮，支持从文本文件读取 `00:00:00-00:00:00（标题）` 格式，一行自动生成一个切片并填入标题。
- 字幕导出时同步生成 `.srt.zip`，页面优先下载 ZIP，避免浏览器或安全策略直接拦截 `.srt` 下载。
- SRT 文件仍可直接读取进入合并短句、智能分段、AI 检查、翻译和导出流程。
- 下载内容新增“翻译后简介”：勾选后调用下方翻译/简介 API，按 B站格式生成中文简介，保存到项目 `04_简介/translated_bilibili_description.txt`；手动“生成简介”也保存到对应项目目录。
- 简介翻译增强：生成“翻译后简介”时会先输出标题翻译，包含英文原题、中文标题、中英文对照和翻译理由；标题翻译会结合原始简介、标签、人物/公司/产品背景和视频主题，不只做直译。
- 在线字幕工作台新增“翻译参考资料”：可粘贴资料、关键词或搜索结果，调用翻译模型整理成 `10_翻译/translation_reference.md`，之后 AI 检查、翻译、一键生成和对比翻译都会带入这份参考；即使尚未读取字幕，也可以只输入视频标题、链接、人物、公司、产品或关键词，让支持联网的模型先搜索并整理相关翻译参考，普通 API 则会生成建议搜索关键词和待确认项。
- 翻译参考资料新增“人物/专名背景增强”开关，默认开启：整理参考时会重点识别人名、公司、产品、模型名和职业身份；翻译时默认保留这些专属名称英文，并用人物职业背景理解上下文。
- 翻译字幕导出文件名增加可对比后缀：`英文原版`、`无翻译参考资料版`、`有翻译参考资料版`、`对比翻译`，便于同一项目内比较不同翻译策略。
- 在线字幕工作台新增“清除翻译”按钮：只清空当前会话里的中文字幕和对比译文，保留识别/校正后的英文，不删除已经导出的字幕文件，方便重新翻译做对比。
- 新增“双版本字幕评分与交叉审校合并”：上传 A/B 两个双语 SRT 后，先检查编号、时间轴和英文原文对齐，再随机映射为版本 X/Y 交给大模型逐条评分；只审校中文不同的条目，不让模型默认偏向参考资料版，最终输出合并版 SRT、逐条评分 CSV 和汇总 JSON。
- “烧录翻译视频”改为优先使用在线字幕工作台当前上传/翻译好的字幕内容，没有当前字幕会话时才回退到项目最新导出的字幕文件；字幕样式新增字间距和行间距参数，烧录时按新参数生成 ASS。

## 2026-05-31 字幕翻译与烧录修复

- 修复无中文字幕时 AI 翻译失败却继续把英文当中文烧录的问题；现在会明确提示需要填写 AI API 或显示接口错误。
- 自动翻译中文默认开启，双语字幕下载前会检查 AI API 地址和 Key，避免生成只有英文的成品。
- 字幕背景烧录改为独立 ASS 矢量背景层，支持半透明和圆角，不再依赖 ASS 原生方形背景盒。
- 中文字幕会自动兜底使用微软雅黑，避免选择 Arial 等英文字体时中文显示成问号。
- 字幕设置 UI 已约束宽度，字体下拉框、排版按钮、字号和背景滑杆不会再挤出卡片。
- 下载内容改为复选框，可分别选择视频、音频、封面、简介、链接标题；只勾资料项时不会下载视频，勾音频会在 `02_音频` 保留音频文件。
- 音频下载固定为 MP3：直接下载音频和从视频中抽取音频都会输出 `.mp3`。
- MP3 下载后会自动标准化为 Windows 兼容的 MP3：ID3v2.3、44.1kHz、双声道、192kbps，减少播放器提示“文件无效”的情况。
- 字幕工作台增加在线识别方式：火山极速版本地文件直传、火山标准版公网音频 URL。
- 火山极速版会把本地音频/视频转为识别用 MP3，超长文件按分钟自动分段，识别结果继续进入 `raw_en.srt`、AI 单词检查、翻译和双语字幕流程。
- 明确区分两套 API：识别 API 只填火山语音识别 Key；翻译 API 填聊天/翻译大模型 Key，用于检查、校正和翻译。
- 在线字幕工作台增加“智能分段”按钮：识别后、翻译前可按语义拆开过长英文字幕，生成 `split_en.srt`，减少双语字幕一条过长的问题。
- “一键生成”流程更新为：识别、保存 raw_en、合并短句、智能分段、AI 检查、应用校正、翻译、导出所选字幕。
- 在线字幕工作台、下载、简介和文件列表按钮增加鼠标悬停说明。
- 新增手动视频切片：上传并读取视频后，可添加多个起止时间段，导出多个 MP4 到 `40_切片`，并按当前字幕生成对应 SRT。
- 视频切片区域改为粘贴本地视频路径；只记录源视频地址，不复制源视频，切片输出到原视频同目录的 `<视频名>_clips` 文件夹。
- “清空全部”改为清空 `downloads` 下的下载文件和项目输出，并重置当前本地字幕会话。
- 新增“对比翻译”开关，可配置第二套翻译 API，同时生成主译文和对比译文；对比译文在预览中显示，并保存为 `compare_zh.srt`。

## 1. 项目边界

- 项目工作目录固定为：`N:\C\Youtube`
- 程序文件、下载文件、字幕文件、切片文件都应放在这个目录或它的子目录里。
- 外部来源文件可以读取，但不直接修改原件。需要处理时，先复制一份到本项目目录。
- C 盘只可能出现 Python、Codex、系统缓存或运行环境路径，不作为本项目资料保存位置。

## 2. 当前已经完成

### 字幕烧录

- 修复了双语字幕烧录后背景框和文字不对齐的问题。
- 字幕背景改为由 libass 原生背景盒绘制，减少手动计算字体高度造成的偏移。
- 烧录前会读取视频真实分辨率，让字幕坐标按视频尺寸生成。

### 在线字幕工作台

已增加“在线字幕工作台”，用于处理本地视频、音频或已有 SRT 字幕。

当前流程：

1. 读取本地视频、音频或 SRT。
2. 如果是视频或音频，只使用在线 API 识别。
3. 合并短句。
4. 智能拆分过长字幕，控制单条字幕长度。
5. 使用大模型检查英文单词、专有名词、大小写和标点。
6. 应用校正结果。
7. 翻译中文字幕。
8. 导出英文、中文或中英双语字幕。
9. 支持选择英文在上或中文在上。

### 启动方式

- 新增 `打开网页版.bat`，用于直接打开网页版本。
- 新增 `open_web.py`，用于启动本地网页服务并打开浏览器。

### 录音识别测试

已测试文件：

- 原始微信文件：`J:\WWW\xwechat_files\wxid_r2lzhmvyamcs22_dd78\msg\file\2026-05\新录音 3.m4a`
- 项目内保留副本：`N:\C\Youtube\downloads\local_sources\20260530_新录音_3\source.m4a`
- 识别字幕：`N:\C\Youtube\downloads\local_sources\20260530_新录音_3\source.srt`

这次测试确认：原文件不动，先复制到项目目录，再生成字幕文件。

## 3. 目标工作流

目标是把程序逐步更新成一个完整的视频处理流水线：

1. 下载或读取源视频。
2. 保留源文件和原始信息。
3. 识别英文字幕，生成带时间轴的字幕文件。
4. 合并短句。
5. 在线大模型检查英文识别错误。
6. 翻译中文字幕。
7. 导出字幕文件。
8. 生成带字幕的视频成品。
9. AI 根据内容分析切片点。
10. 自动生成 10 个候选切片，每个片段 2-5 分钟。
11. 用户手动调整切片内容。
12. 程序把最终切片选择和处理记录写回项目资料。

## 4. 每个视频项目建议结构

未来建议每个视频单独一个项目文件夹，例如：

```text
downloads\projects\20260530_video_name\
  00_源文件\
    source_meta.json
    thumbnail.jpeg
    original.info.json

  01_视频\
    source.mp4

  02_音频\
    source.audio.mp3

  03_字幕\
    platform.en.vtt
    raw_en.srt

  04_简介\
    bilibili_description.txt
    translated_bilibili_description.txt
    timeline_outline.md

  10_翻译\
    translation_reference.md
    zh.srt
    bilingual.en-top.srt
    bilingual.zh-top.srt

  30_烧录\
    full_with_subtitles.mp4

  40_切片\
    clip_candidates.json
    clip_01.mp4
    clip_01.srt
    clip_01.md

  90_logs\
    process_log.md
    update_notes.md
```

说明：

- `00_源文件` 保存源文件和原始信息。
- `01_视频` 保存源视频。
- `02_音频` 保存抽取或下载的音频，供在线识别使用。
- `03_字幕` 保存平台字幕和原始识别字幕。
- `04_简介` 保存原始 B 站格式简介、翻译后 B 站简介和时间轴大纲。
- `10_翻译` 保存识别、校正、翻译和双语字幕。
- `30_烧录` 保存完整带字幕视频。
- `40_切片` 保存切片候选、最终切片和切片字幕。
- `90_logs` 保存本项目每一步处理记录。

## 5. 原始信息需要保存的内容

每个视频项目都应该尽量保存这些信息：

- 原始标题
- 原始简介
- 原始链接
- 来源平台，例如 YouTube、B 站、本地上传
- 频道名称
- 发布时间
- 下载时间
- 原始字幕或平台字幕
- 语音识别结果
- AI 校正结果
- 翻译结果
- 字幕烧录结果
- 切片候选
- 最终选择的切片
- 用户手动调整记录

## 6. 在线识别计划

当前只保留在线识别。火山支持两种入口：

- 火山极速版：本地文件直传，程序可自动转 MP3 并按片段提交，适合当前“上传音频识别字幕”的流程。
- 火山标准版 URL：适合已有公网音频 URL 的长文件；本地文件若要走标准版，后续需要再接对象存储上传。
- Resource ID 可在界面手动调整：极速版默认 `volc.bigasr.auc_turbo`；标准版 1.0 默认 `volc.bigasr.auc`，标准版 2.0 可改为 `volc.seedasr.auc`。
- 阿里云百炼 / Paraformer / Fun-ASR：适合带时间戳的字幕工作流。

后续优先级建议：

1. 实测火山极速版英文 AI 术语准确率。
2. 如果标准版长文件效果更好，再接火山 TOS/公网临时 URL。
3. 识别结果统一转成 SRT。
4. 后续流程仍然走合并短句、AI 检查、翻译、导出。
5. 不让大模型修改时间轴，只让它修改文字。

## 7. 切片计划

切片功能目标：

- 根据完整字幕分析内容结构。
- 自动生成 10 个候选切片。
- 每个候选片段长度控制在 2-5 分钟。
- 切点优先选择话题自然转换处。
- 每个切片生成标题、摘要、关键词、起止时间。
- 用户可以调整切片起止时间和标题。
- 调整后的切片信息保存回项目资料。

候选切片记录格式建议：

```json
[
  {
    "index": 1,
    "title": "片段标题",
    "start": "00:03:10",
    "end": "00:06:45",
    "summary": "这一段讲了什么",
    "keywords": ["关键词1", "关键词2"],
    "selected": false,
    "user_note": ""
  }
]
```

## 8. 更新记录

### 2026-05-30

- 新增本更新文档。
- 新增 `README.md`，用于 GitHub 首页说明。
- 新增 `.gitignore`，默认不上传视频、音频、缓存、日志和本机工具配置。
- 明确项目只在 `N:\C\Youtube` 内保存和更新。
- 明确源文件保留规则：外部文件先复制到项目目录，原件不动。
- 记录在线字幕工作台当前能力。
- 记录未来项目文件夹结构。
- 记录在线识别、字幕成品、切片候选的下一步方向。

### 2026-05-31

- 下载流程改为按视频标题创建项目文件夹：`downloads/projects/<视频标题>/`。
- 每个项目内固定整理：源信息、视频、音频、字幕、B 站简介、时间轴大纲、翻译结果、切片目录。
- 下载视频后会自动抽取一份音频，后续可直接用于在线语音识别。
- 在线字幕工作台增加“项目/视频标题”，上传音频、视频或 SRT 后会进入对应项目文件夹。
- 识别和翻译后的文件统一保存到项目内 `10_翻译` 文件夹。
- AI 单词检查结果改为可逐条查看、手动调整，再一键应用校正。
- 导出字幕支持英文、中文、双语；双语字幕支持中文在上或英文在上。

## 9. 下一步开发清单

- 增加“新建视频项目”功能，自动创建项目文件夹。
- 下载视频后自动保存源文件和 `source_meta`。
- 本地上传视频后也进入同样的项目文件夹结构。
- 继续实测并微调火山在线英文识别参数。
- 为火山标准版增加对象存储/临时公网 URL 上传能力。
- 合并短句后生成 `merged_en.srt`。
- AI 校正后生成 `corrected_en.srt` 和校正报告。
- 翻译后生成中文字幕和双语字幕。
- 增加字幕效果预览。
- 增加完整视频烧录导出。
- 增加 AI 内容分析和 10 个切片候选。
- 增加切片候选手动调整界面。
- 选中切片后导出剪辑文件和对应字幕。
